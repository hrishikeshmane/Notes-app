import "./Header.css";
import NotesIcon from "@mui/icons-material/Notes";

const Header = () => {
  return (
    <header>
      <NotesIcon />
      <h3>Notes App</h3>
    </header>
  );
};

export default Header;
